---
name: kritik
version: 0.1.0
description: >
  Audit this product's quality with the Kritik framework — score each criterion
  on each surface against observable maturity anchors, record findings with
  their risk and remediation cost, and roll the result up into a comparative
  matrix. Use this skill when asked to audit, review, or assess the quality,
  security, privacy, accessibility, performance, reliability, or test coverage
  of a codebase; when asked "how good is this product", "what should we fix
  first", or "where are we weakest"; when preparing a milestone quality gate;
  and when a previous audit needs re-running, extending to a new surface, or
  checking for regressions between audits.
---

# Kritik — quality auditing

You are auditing a product with **Kritik**: a framework that answers one
question repeatedly and comparably — *how good is each surface of this product,
on every dimension that matters, and what exactly do we fix first?*

Two kinds of record come out of an audit, and they are not the same thing:

- an **assessment** scores one *criterion* on one *surface* 0–4, with evidence.
  It says how systematically a concern is handled.
- a **finding** is one concrete defect, carrying `impact`, `likelihood`, and a
  remediation `cost`. It says what is actually wrong.

A surface can be maturity-3 and still hold a Critical finding. That is not a
contradiction, it is the point — and the roll-up rules below make sure the
finding cannot be averaged away.

## Where everything lives

```
docs/quality/
  profile.json            # THIS project: its surfaces and domain weights
  criteria.custom.json    # THIS project: its own criteria, layered over the pack
  audits/<YYYY-MM>/
    scores.json           # assessments for one run
    findings.json         # findings for one run
    matrix.json           # GENERATED by compute-matrix.js — never hand-written
```

These sidecar files are **canonical in a repository**, exactly as the journal's
JSONL sidecar is canonical while a bundle's embedded `journal[]` is only the
interchange projection. If this repo also has an Arkaik map, the bundle's
`quality` section is that same kind of projection — produced from these files,
never edited in place instead of them.

The framework itself ships beside this skill and is read-only:

| File | What it is |
|---|---|
| `references/framework.md` | The meta-model: the scales, the roll-up rules, the audit process |
| `references/library.json` | The criteria pack: domains, criteria, anchors, checklists, signals, issue skeletons |
| `scripts/compute-matrix.js` | The roll-up. Zero dependencies — `node scripts/compute-matrix.js <audit-id>` |
| `scripts/scaffold-criterion.js` | Adds a project-specific criterion to the overlay |
| `scripts/init-profile.js` | Writes `profile.json` — the install-time surface picker |

Nothing here needs `node_modules`. Every script runs with nothing but Node.

## First: does this project have a profile?

**If `docs/quality/profile.json` does not exist, stop and create it before
scoring anything.** Without it there is no surface list, so there is nothing to
score *against* and no columns for a matrix.

A **surface** is one independently assessable body of code. It is deliberately
not the same vocabulary as an Arkaik platform: `web`, `ios`, and `android` name
where a *view* ships, while an admin back-office or a database contract is an
audit target that ships no views at all.

Ask the user, one question at a time:

1. **Which surfaces does this product have?** Look at the repo first and propose
   a list rather than asking cold — a monorepo's `apps/*`, a single Next.js app,
   a mobile client, a database package. A single-surface project picks one, and
   every cell collapses to one column; that is normal, not a degenerate case.
2. **Does any surface ship views?** If so, map it to `web`, `ios`, or `android`
   so quality can cross-link to the product map later. Leave the mapping off
   where it does not apply — do not invent one.
3. **What is this product graded hardest on?** Domain weights, defaulting to 1.
   An intimate-data product weights `SEC` and `PRV` above the rest; an internal
   tool probably does not. Only ask if the user has an opinion; 1 across the
   board is an honest default.

Then write it:

```
node scripts/init-profile.js \
  --surface web:Web app:web \
  --surface supabase:Database contract \
  --weight SEC=2 --weight PRV=2
```

Or hand it a config you already assembled: `--config <path.json>`. Re-running is
safe: it refuses to clobber an existing profile unless you pass `--force`.

> **`cross-surface` is reserved.** It is the contract *between* surfaces — schema
> and RPC symmetry, client-to-client payload tolerance, mirror parity. Findings
> may carry it; it holds no assessments and never becomes a matrix column. Do not
> declare it as a surface in the profile.

## The scales

Read `references/framework.md` for the full anchors. The short form:

**Maturity (0–4)** — what an assessment scores. It measures *how systematically*
a concern is handled, never how it feels:

| | |
|---|---|
| **0** Absent | Not addressed at all |
| **1** Ad-hoc | Addressed accidentally or in one spot; no visible intent |
| **2** Defined | Deliberately addressed; visible intent; known gaps |
| **3** Managed | Systematic across the surface; tested or reviewed |
| **4** Verified | Enforced by automation or a CI gate; drift is *detected*, not hoped against |

"The code happens to be fine today" is a 2. "It cannot regress without failing
the build" is a 4. Half-points do not exist — when torn, **score the lower one
and say why in the evidence.**

A criterion that does not apply to a surface has **no row at all**. N/A is the
absence of an assessment, never a level 0.

**Risk** — what a finding carries. `impact` (1–5) is the worst plausible
consequence; `likelihood` (1–5) is how probable that is under real use or
attack. `cost` is `S` / `M` / `L` / `XL`.

**Do not write `severity` or `priority` into a finding.** They are derived from
those three by `compute-matrix.js`, and storing them is how a matrix starts
disagreeing with itself. The validator warns if you do.

## Running an audit

### 1. Pin the snapshot

One commit hash. Every piece of evidence in the run refers to it. Record it in
both JSON files as `commit`, and pick an `audit_id` — `YYYY-MM` is the
convention (`2026-08`).

### 2. Run the repo's own gates first

Lint, typecheck, tests, build. Record what they said. **An audit must never
silently contradict CI**: if the test suite is green and you are about to score
testing a 1, you need evidence that explains both, and if the suite is red that
is itself a finding.

### 3. Score, one criterion at a time

For each criterion in the pack whose `applies_to` intersects your profile's
surfaces, on each of those surfaces:

- Run its `checklist` — the greps, files, and flows are written to be executed,
  not skimmed. Do them.
- Read its `level_anchors` and pick the level whose description actually matches
  what you found.
- Write the evidence: `file:line` citations, config keys, the command you ran
  and what it returned.

**A score without evidence is an opinion, not an assessment.** This is the hard
rule of the framework, and it is the one to hold when a score feels obvious —
"the auth looks fine" is not an assessment; `middleware.ts:14-31 validates the
session server-side on every protected route group` is.

Two disciplines that decide whether the numbers mean anything:

- **Score what the code *is*, not what an open PR promises.** Improvements land,
  then the score moves. That is what makes a trend real.
- **Score the same criterion the same way on every surface.** Comparability
  across a row is the whole reason it is a matrix.

Write each row into `docs/quality/audits/<id>/scores.json`:

```json
{
  "audit_id": "2026-08",
  "commit": "<sha>",
  "framework_version": "0.1.0",
  "assessments": [
    { "criterion_id": "SEC-01", "surface": "web", "level": 2,
      "evidence": "middleware.ts:14-31 …", "audit_id": "2026-08",
      "commit": "<sha>", "ts": "2026-08-26T00:00:00.000Z" }
  ]
}
```

### 4. Emit findings

Emit a finding for everything you found at or below its target level. One
finding is one defect — not one criterion, and not one surface's worth of
grumbling. It carries what someone needs to act:

```json
{ "id": "F-2026-08-SEC-web-01", "criterion_id": "SEC-01", "surface": "web",
  "title": "One line naming the actual defect, not the category",
  "detail": "What breaks, for whom, and the path that gets there.",
  "evidence": "file:line citations",
  "impact": 4, "likelihood": 3, "cost": "S", "status": "open",
  "remediation": "The fix, concretely.",
  "node_ids": ["V-settings"] }
```

`node_ids` is optional and ties the finding to the Arkaik product graph when
this repo has a map — use the real node ids, never guessed ones.

### 5. Verify Critical and High adversarially

Before a Critical or High finding is retained, **try to refute it** with the repo
in front of you. Look for the guard you missed, the migration that already fixed
it, the caller that cannot reach the path. Record the verdict:

```json
"verification": { "verdict": "CONFIRMED", "note": "what you re-checked and found" }
```

`CONFIRMED`, `REFUTED`, or `DOWNGRADED`. **A refuted finding is disclosed, not
deleted** — set its `status` to `refuted` and leave it in the file. A reader
learning what you checked and dismissed is a reader who can trust the rest.

### 6. Roll up — this is the gate

```
node scripts/compute-matrix.js 2026-08
```

It writes `matrix.json` and prints the matrix. It is the only thing that may
write that file. If it errors, **fix the data — never hand-edit the output.**

What it does, so you can read the result: a domain's score on a surface is
`Σ(level × weight) / Σ(4 × weight) × 100` over the criteria you actually scored;
grades are A ≥ 85, B ≥ 70, C ≥ 55, D ≥ 40, else E; and the **anti-averaging
caps** apply — one open Critical caps its cell at **D**, one open High caps it
at **B**, shown with an asterisk. A cell full of 3s does not get to absorb a
live account-takeover.

### 7. Record it in the journal

If this repo keeps an Arkaik journal, append one `quality.audit.completed` plus
one `quality.finding.opened` per retained finding. **Always set `actor`** — a
trend is only filterable by assessor kind if every writer says who it was.

```json
{"id":"<ULID>","ts":"<ISO>","actor":"claude-code","type":"quality.audit.completed","audit_id":"2026-08","framework_version":"0.1.0","commit":"<sha>","scores":{"web":{"SEC":44}},"counts":{"critical":0,"high":21,"medium":58,"low":12}}
{"id":"<ULID>","ts":"<ISO>","actor":"claude-code","type":"quality.finding.opened","finding_id":"F-2026-08-SEC-web-01","criterion_id":"SEC-01","surface":"web","severity":"high","priority":"P1","title":"…"}
```

When the fix merges, append `quality.finding.resolved` with `resolved_by` set to
the PR URL. Events are facts, never state: the current picture is always the
latest audit plus opened-minus-resolved, computed, not stored.

### 8. File issues for P0 and P1

Each criterion in the pack ships an `issue` skeleton — `title_template`,
`labels`, `body_skeleton`. Fill every `{placeholder}` and file. P2/P3 batch at
the maintainer's discretion; do not open sixty issues nobody asked for.

## Between audits: signals

Every criterion carries `signals[]` — mechanically checkable hooks. A grep that
must return nothing; a CI job that must exist. They are cheap, so run them
between full audits and append `quality.signal.tripped` when one regresses.
A tripped signal is not a finding; it is the prompt to go look.

## Adding a criterion of your own

The pack is generic on purpose. When this product has a quality concern the pack
does not name — a domain rule, a house convention that matters, a regulation
specific to your market — add it:

```
node scripts/scaffold-criterion.js
```

It asks for the pieces, writes the criterion into
`docs/quality/criteria.custom.json`, and generates its issue skeleton alongside.
The overlay is layered over the pack at load, so **a pack upgrade never touches
your criteria** — that separation is the whole reason the overlay exists.

Give a custom criterion the same rigor as a pack one, or it will not survive
contact with a real audit: a real `question`, five `level_anchors` describing
things you could *observe*, an honest `weight` (1–3), and at least one `signal`.
Use a project-reserved id namespace (`X-01`, `X-02`, …) so it can never collide
with a future pack criterion. It may attach to an existing domain or define its
own.

## Scoping a run

You will not always audit everything, and you should not pretend otherwise.

- **A full audit** covers every applicable (criterion × surface) cell. It is a
  milestone activity — for a large monorepo, fan out one auditor per
  (surface × domain cluster), add a pass dedicated to the `cross-surface`
  contract, then verify Critical/High adversarially before rolling up.
- **A partial audit** is legitimate and common: one surface, one domain, or the
  criteria touched by a release. Score only what you actually checked. **A cell
  you did not look at gets no row** — leaving it out reads as "not assessed",
  while inventing a level reads as fact and is worse than silence.

Either way, say which you did when you report back.

## Reporting back

Lead with what to fix first, not with the matrix. The matrix is the evidence; the
P0 list is the answer. State the number of assessments and findings, the caps
that fired and why, and anything you could not assess and the reason. If a
surface came out unexpectedly well, say so — an audit that only ever finds
problems is one nobody believes the second time.
